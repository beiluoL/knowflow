# 项目日志

## 2026-07-31 证书获得庆祝弹窗 + 章节完成按钮布局收敛

### 功能概述
学习路径完成时不再「无感获得证书」：完成最后一章（路径达成 100%）后弹出带彩带动效的全屏「恭喜获得证书」庆祝弹窗，可跳转证书页查看；同时将「标记本章已完成」按钮从正文底部的大号醒目块移入底部固定操作栏，与「下一章」并排、适度强调。

### 前端（Vue 3 + TS）
- 新增全局庆祝子系统：
  - `utils/celebrate.ts`：仿照 `toast.ts` 的 reactive 全局状态 + 函数式 API，提供 `celebrateCertificate(cert)` / `dismissCelebration(id)`
  - `components/ui/Celebration.vue`：全屏庆祝弹窗宿主（金色奖章 + 证书信息 + 彩带飘落 CSS 动效 + 「查看证书」跳 `/certificate/:id` + 「稍后查看」），挂载到 `App.vue`
- `ChapterLearn.vue`：
  - 移除正文底部 `.chapter-complete-bar` 大号醒目块，完成操作移入底部固定操作栏「下一章」旁：未完成未锁定→「标记本章已完成」按钮（品牌绿描边 + 细阴影，适度强调）；已完成→「本章已完成」徽标；已锁定→解锁提示
  - 完成**最后一章**后调 `checkPathCertificate()`：查 `learningApi.certificates()` 按 pathId 找证书（幂等同路径单证书），命中则 `celebrateCertificate` 触发庆祝，避免无感获得证书

### 设计决策
- 庆祝特效聚焦「获得证书」这一明确场景（用户确认）：成就解锁、等级提升、称号进阶保持现有 notify 轻提示，不做每次章节完成后的拉取比对
- 彩带动效用纯 CSS `@keyframes` 实现，不引入外部特效库；色值取自 `kb-*` 变量 + 有限庆祝色板

### 同步文档
- `README.md`：章节学习路由说明补充「底部栏完成按钮 + 证书庆祝弹窗」
- `docs/项目日志.md`：本条目

### 校验
- 前端 `Celebration.vue` / `celebrate.ts` / `ChapterLearn.vue` / `App.vue` lint 通过
- 后端无改动

---

## 2026-07-31 学习路径列表页报名入口 + 章节完成按钮增强

### 功能概述
在「学习路径列表页」补齐一键报名能力，形成「列表报名 → 开始学习」的完整闭环；同时增强章节页底部「标记本章已完成」按钮的视觉醒目度。

### 后端（Spring Boot 3 + MyBatis-Plus）
- `LearningPathVO` 新增 `progress` 字段（当前登录用户学习进度百分比 0~100，未报名时为 null）
- `LearningService.getPathList` 签名扩展为 `getPathList(Long userId)`：已登录时批量查询 `learning_user_path`，填充 `enrolled` 与 `progress`；匿名访问（userId 为 null）时不填充
- `LearningController.paths` 改用 `SecurityUtils.getCurrentUserIdNullable()` 获取可空 userId（列表接口保持匿名可访问）

### 前端（LearningPaths.vue + types.ts）
- `LearningPathVO` 类型补充 `progress` 字段
- 路径卡片进度行改为三态：未报名 →「报名学习」按钮（强调色 + loader 加载态）；已报名未开始 →「开始学习」；已报名且有进度 → 进度条 +「学习中」
- `enrollPath`：列表内直接调 `learningApi.enroll`，成功后原地更新 `enrolled=true` 使按钮切换为「开始学习」，带 `enrollLoadingIds` 防重复点击
- `ChapterLearn.vue` 章节完成区：完成按钮改为大尺寸强调色 + 阴影，未完成态/已完成态/锁定态均以胶囊徽章样式突出显示

### 同步文档
- `README.md`：学习路径列表页路由说明补充「列表内一键报名」
- `docs/项目日志.md`：本条目

### 校验
- 后端 `mvn compile` 通过；前端 `LearningPaths.vue` / `ChapterLearn.vue` / `types.ts` lint 通过

---

## 2026-07-31 学习路径报名交互（显式报名按钮 + 报名后解锁）

### 功能概述
补齐学习路径详情页缺失的「报名」入口，形成「报名 → 解锁章节 → 开始学习」完整闭环，解决未报名时完成章节报「请先报名学习路径」的体验断点。

### 后端（Spring Boot 3 + MyBatis-Plus）
- `LearningPathVO` 新增 `enrolled` 字段（当前用户是否已报名）
- `LearningService.getPathDetail` 签名扩展为 `getPathDetail(pathId, userId)`，内部查询 `learning_user_path` 是否存在 `(userId, pathId)` 记录以判断报名状态
- `LearningController.pathDetail` 从 `authentication.getPrincipal()` 取 userId 传入
- 报名幂等逻辑沿用既有 `enrollPath`（先查后插），未改动

### 前端（PathDetail.vue + types.ts）
- 主按钮状态机：未报名 →「报名学习」（描边样式 + `user-plus` 图标）；已报名未开始 →「开始学习」；学习中 →「继续学习」；已完成 →「复习路径」+ 证书入口
- 未报名引导横幅：Hero 下方虚线卡片「报名后即可解锁全部章节」，一键「立即报名」
- 章节锁定：未报名时时间轴与 DAG 图谱全部锁定，点击提示「请先报名学习路径」
- `handleEnroll`：调 `learningApi.enroll` → 成功后重新拉取详情刷新 `enrolled`、章节解锁、按钮切换为「开始学习」，带 loading 防重复点击

### 同步文档
- `DATABASE.md` / `README.md`：`LearningPathVO` 新增 `enrolled` 字段说明（学习路径详情接口返回报名状态）
- `docs/项目日志.md`：本条目

### 校验
- 后端 `mvn compile` 通过；前端 `PathDetail.vue` / `types.ts` lint 通过

---

## 2026-07-31 学习门户增强 + 数字证书 + 知识库邀请接通 + PRD 对齐

### 功能概述
一次落地四类迭代：① 接通知识库邀请接口（填补前端两处 TODO）；② 学习门户增强 L-FORM-01/02（章节嵌入视频 + 互动讲义）与 A-FB-01/02（相关概念/文档推荐 + 章节内嵌即时测验）；③ G-CERT-01 数字证书完整形态（后端自动颁发 + 验证码 + 前端证书页 + canvas 下载）；④ 修正 PRD 中成就系统/知识图谱的标注偏差。

### ① 知识库邀请接口接通
- `KnowledgeCreate.vue`：创建知识库后接收返回的 `id`，遍历表单中已填邮箱的编辑者成员，映射中文「编辑者」→ 后端 `EDITOR`，逐个调用 `adminApi.addKbMember` 批量邀请（跳过 `isOwner`）
- `KnowledgeHome.vue`：邀请对话框由 TODO 改为真实调用 `adminApi.addKbMember`（`categoryId = activeCategoryId`），成功后提示并关闭

### ② 学习门户增强（L-FORM-01/02 + A-FB-01/02）
- **L-FORM-01 章节嵌入视频**：后端 `learning_user_chapter` 新增 `video_progress DECIMAL(5,2)` 字段 + `POST /api/learning/chapters/{id}/video-progress`（单调不减，首次观看自动建记录）；前端正文约定 `[video](url)` 语法渲染外链 iframe（YouTube/B 站自动转 embed），观看停留计时每 20s 上报，≥90% 提示可完成章节
- **L-FORM-02 互动讲义**：正文约定 ` ```js-run ` 代码块浏览器端 `new Function` 执行并输出；` ```quiz-run ` 代码块即时判分
- **A-FB-01 相关推荐**：章节学习页右侧新增「相关概念」标签（从标题分词提取）+「相关文档」列表（按章节标题关键词经 `docsApi.list` 检索，点击跳 `/doc/:id`）
- **A-FB-02 章节内嵌测验**：通过 ` ```quiz-run ` 约定语法在每节末尾内嵌即时测验，提交后即时判分 + 解析反馈

### ③ G-CERT-01 数字证书（完整形态）
- **后端**：新增 `learning_certificate` 表（cert_no 唯一验证码 + 路径/用户名快照 + 联合唯一幂等）；`completeChapter` 检测路径全部章节完成时自动颁发；接口 `GET /api/learning/certificates`（我的列表）、`/{id}`（详情）、`/verify?certNo=`（匿名核验）
- **前端**：新增 `Certificate.vue`（canvas 绘制证书图 + 下载 PNG + 验证码核验区）；`Profile.vue` 新增「我的证书」区块；`PathDetail.vue` 路径完成时展示「查看证书」入口；路由 `/certificate/:id`
- **颁发通知**：路径完成颁发证书后发送站内通知

### ④ PRD 文档偏差修正
- 3.5.1 游戏化学习：成就系统由「mock/缺失」改为「已实现」（achievement + 自动解锁 + `Achievement.vue` 真实 API）
- P3 清单：L-FORM-01/02、A-FB-01/02、G-CERT-01 标 ✅ 已实现；A-RAG-04 补标 ✅（正文已注明落地）
- 3.5.3 作品与证书：由「未实现」改为「部分实现（证书已落地）」
- 前端关键页面表：成就/打卡去掉 `（mock）` 标注

### 同步文档
- `DATABASE.md`：新增 `learning_certificate` 表（#44）+ 更新 `learning_user_chapter` 的 `video_progress` 字段、关系图与表总数（44 张）
- `README.md`：更新 `LearningController` 能力说明（视频进度/证书）、章节学习路由说明（互动讲义）、新增证书路由
- `docs/项目日志.md`：本条目

### 校验
- 后端 `mvn compile` 通过；前端各改动文件 lint 通过

---

## 2026-07-31 社区讨论改造：发布帖子 / 帖子详情独立页面化（替代弹窗）

### 功能概述
将社区讨论中原本以弹窗（Modal / Drawer）承载的「发布帖子」与「帖子详情 + 评论」重构为独立路由页面，点击直接跳转，并提供返回交互。

### 前端（Vue 3 + TS + Vue Router）
- 新增路由：`/community/post/new`（发布页，`requiresAuth: true`）、`/community/post/:id`（详情页），均 `layout: 'c'`
- 新增页面 `views/PostEdit.vue`：标题 / 分类（标签切换）/ 正文表单、标题字数提示、底部常驻发布按钮、右上角「返回」按钮（`router.back()`，无历史时兜底 `/community`）
- 新增页面 `views/PostDetail.vue`：帖子正文、精华 / 浏览 / 评论计数、点赞（幂等切换）、评论列表分页、发表 / 删除评论、骨架屏 / 空态、右上角「返回」按钮
- `views/Community.vue`：移除发布弹窗与详情抽屉，列表项与「发布帖子」按钮改为 `router.push` 跳转；删除 `showPostModal` / `selectedPost` / `openPost` / `closePost` / `handleSubmitPost` 等弹窗相关状态与函数，移除 `useAuthStore` / `CommentVO` 依赖
- 返回交互：页面内「返回」按钮优先 `router.back()` 以保留社区浏览位置，无历史时兜底 `/community`

### 校验
- 前端 `vue-tsc --noEmit` 通过；Vite HMR 已热更新，无需重启
- 后端无需改动（复用 `/community/posts` POST/GET 与评论 / 点赞接口）

## 2026-07-31 学习门户升级：DAG 交互式图谱（dagre）+ AI 推断依赖（L-PATH-04）+ 路径详情页重构

### 功能概述
在 L-PATH-03 基础上完成三项升级：
1. **DAG 渲染引擎替换**：由 Mermaid 静态图升级为 **dagre 分层布局 + 自绘 SVG/HTML 交互式图谱**，支持拖拽平移、滚轮缩放（以光标为锚点）、适应画布、全屏查看、悬停高亮相邻链路并展示章节详情卡片、点击节点直达章节。
2. **AI 自动推断章节依赖（L-PATH-04）**：生成个性化路径时由 AI 推断每章的知识前置依赖，采用路径后落地为 `learning_chapter.prerequisite_chapter_ids`，生成的路径天然带有 DAG 分支结构。
3. **路径详情页视觉重构**：Hero 卡片（主题渐变 + 光晕 + 环形进度）、四宫格数据看板、图谱面板、章节时间轴（三态轴点 + 依赖提示 + 入场动画）。

### 后端（Spring Boot 3 + MyBatis-Plus）
- `PersonalizedPathVO.RecommendChapter` 新增 `prerequisiteSortOrders`（前置章节序号数组）
- `LearningServiceImpl.callAiForPersonalizedPath`：System/User Prompt 增加 DAG 推断要求（只能引用更小 sortOrder、首章为空、鼓励并行分支与多前置综合章节），并解析该字段；AI 未给出依赖时由 `fillLinearPrerequisitesIfAbsent` 退化为线性链
- `LearningServiceImpl.adoptPersonalizedPath`：改为「先插入章节取自增主键 → 建立 sortOrder→id 映射 → 回填 `prerequisite_chapter_ids`」两阶段落地；`resolvePrerequisiteIds` 过滤自引用与后向依赖，从拓扑上保证无环
- `buildDefaultPersonalizedPath`：兜底路径同样带线性依赖

### 前端（Vue 3 + TS + Pinia）
- 新增依赖 `@dagrejs/dagre`（仅做布局计算，渲染完全自绘，便于复用 `kb-*` 主题）
- 新增组件 `components/Learning/DagGraph.vue`：dagre 分层布局（rankdir=LR）、Catmull-Rom 平滑贝塞尔连线、三态节点卡片与箭头 marker、可学边流动虚线动画、缩放/平移/适应画布/全屏工具栏、`ResizeObserver` 自动适配
- `views/PathDetail.vue` 整页重构，接入 `DagGraph` 替换 `MermaidDiagram`；章节时间轴展示前置章节标题
- `views/LearningPaths.vue`：AI 生成结果的章节规划中展示「前置：第 N 章」
- `api/types.ts`：`RecommendChapter` 新增 `prerequisiteSortOrders`

### 校验
- 后端 `mvn compile` 通过；前端 `vue-tsc --noEmit` 与 `vite build` 均通过，lint 0 错误
- `MermaidDiagram` 仍被 `KnowledgeGraph.vue` 使用，故保留 `mermaid` 依赖

## 2026-07-31 实现学习门户：章节依赖关系图（DAG 可视化，L-PATH-03）

### 功能概述
在「学习路径详情页（PathDetail）」新增 **学习路径图谱** 卡片，将章节的 `prerequisiteChapterIds` 前置依赖渲染为 **Mermaid 流程图（DAG）**：节点按完成 / 可学 / 锁定三态着色，边表示「前置章节 → 当前章节」依赖；AI 路径可呈现真实分支（如「深度学习入门」同时指向「计算机视觉」「自然语言处理」）。

### 后端（Spring Boot 3 + MyBatis-Plus）
- VO：`ChapterDagVO` / `ChapterNodeVO` / `ChapterEdgeVO`
- 服务：`LearningService.getChapterDag(pathId, userId)` —— 复用 `getChapterList` 的前置解锁规则计算节点状态（completed / available / locked），由 `prerequisiteChapterIds` 解析出「仅限本路径内」的依赖边（过滤悬空边）
- 控制器：`LearningController` 新增 `GET /api/learning/paths/{pathId}/dag`（需登录）
- 种子数据：`data.sql` 已补 `prerequisite_chapter_ids`，演示线性链与分支（AI 路径 15→16/17 形成真实 DAG）

### 前端（Vue 3 + TS + Pinia）
- `api/types.ts` 新增 `ChapterDagVO`/`ChapterNodeVO`/`ChapterEdgeVO`/`ChapterNodeStatus`；补全 `LearningChapterVO` 缺失的 `locked`/`prerequisiteChapterIds` 字段
- `api/learning.ts` 新增 `dag(pathId)`
- `PathDetail.vue`：新增「学习路径图谱」卡片（图例 + Mermaid 渲染 + 加载 / 错误 / 空态 + 线性路径提示），复用既有 `MermaidDiagram` 组件

### 校验
- 后端 `mvn -o -q compile` 通过；接口冒烟测试通过（AI 路径返回 8 节点 / 8 边，状态计算正确）
- 前端 lint 0 错误；遵循 Airbnb/Vue3 风格（`kb-*` 变量、`Icon.vue` 自绘 `git-branch` 图标、类型化请求封装）
- 文档已同步：`PRD.md`（L-PATH-03 标记已落地、功能现状总览 / 差距分析更新）、`README.md`（LearningController 接口与学习中心说明）

## 2026-07-30 实现沉浸式学习助手：知识图谱升级（A-RAG-04 实体关系抽取）

### 功能概述
将知识图谱从「分类 → 文档」层级图升级为 **AI 从文档正文抽取实体 + 关系构建的真正知识图谱**。用户可在「知识实体图」Tab 一键触发抽取，图谱按实体类型（概念/技术/术语/原理/工具）着色、按关系（前置/属于/组成/使用/对比）连线，点击节点查看说明与关联实体。

### 数据库（schema.sql，H2 内存库随启动初始化）
- `kg_entity` 知识实体表（doc_id/category_id/name/type/description/weight，按 name 全局去重合并）
- `kg_relation` 实体关系表（source_entity_id/target_entity_id/relation/description/doc_id，按 源/目标/关系/文档 去重）
- 文档上传时自动触发抽取（`DocServiceImpl.uploadDoc` 中 best-effort 调用）

### 后端（Spring Boot 3 + MyBatis-Plus）
- 实体：`KgEntity` / `KgRelation`；Mapper：`KgEntityMapper` / `KgRelationMapper`
- VO：`EntityGraphVO` / `EntityNodeVO` / `EntityEdgeVO` / `ExtractResultVO`
- 服务：`KnowledgeService` 新增 `getEntityGraph`（查询）/ `extractDoc`（单篇抽取）/ `extractByCategory`（批量抽取）
- 控制器：`KnowledgeController`（`/api/knowledge`）
  - `GET /entity-graph`（分类/文档维度过滤，匿名可读）
  - `POST /extract`（按分类或全部已发布文档批量抽取）
  - `POST /extract/{docId}`（单篇抽取）
- 抽取：复用 `aiService.complete()`，提示模型返回 `entities[]` + `relations[]` 的 JSON，解析后按名称合并实体、按 源/目标/关系/文档 去重关系

### 前端（Vue 3 + TS + Pinia）
- `api/types.ts` 新增 `EntityNodeVO`/`EntityEdgeVO`/`EntityGraphVO`/`ExtractResultVO` 等类型
- `api/knowledge.ts` 新增 `entityGraph()` / `buildEntityGraph()`
- `KnowledgeGraph.vue`：新增第 4 个 Tab「知识实体图」+「构建知识图谱」按钮，复用 SVG 渲染（圆形布局、按类型着色、关系标签、节点详情与关联实体跳转）

### 校验
- 后端遵循阿里 Java 规范（逻辑外键建索引、禁用物理外键、表名小写、BaseEntity 四件套）；前端遵循 Airbnb/Vue3 风格（`kb-*` 变量、`Icon.vue` 自绘图标、类型化请求封装）
- 文档已同步：`DATABASE.md`（42/43 表 + ER + 关系表）、`README.md`、`PRD.md`（A-RAG-04 标记已落地）

## 2026-07-30 新增「编程挑战」模块（十题闯关 / 积分游戏化）

### 功能概述
新增 C 端「编程挑战」菜单：挑战赛道内含 10 道关卡，逐关解锁；浏览器端执行测试用例判题，后端判定通关并计算星级、积分与解锁；提供积分排行榜与个人战绩统计。

### 数据库（schema.sql / data.sql，H2 内存库随启动初始化）
- `code_challenge` 挑战赛道表（标题/语言/难度/图标/主题色/关卡数/总积分/状态）
- `code_challenge_level` 关卡表（level_no 序号、题目描述、示例、提示、代码模板、测试用例 JSON、积分）
- `code_challenge_record` 用户赛道进度表（通关数/当前关卡/累计积分/星星/是否全通）
- `code_challenge_level_record` 用户关卡记录表（是否通关/星级/尝试次数/所得积分/最近代码）
- 种子数据：1 条「JavaScript 十题闯关」赛道 + 10 道关卡（两数之和 → 经典算法，循序渐进）

### 后端（Spring Boot 3 + MyBatis-Plus）
- 实体：`CodeChallenge` / `CodeChallengeLevel` / `CodeChallengeRecord` / `CodeChallengeLevelRecord`
- Mapper：对应 4 个 BaseMapper
- DTO/VO：`ChallengeSubmitDTO`；`ChallengeVO` / `ChallengeDetailVO` / `ChallengeLevelVO` / `ChallengeSubmitResultVO` / `ChallengeRankVO` / `ChallengeStatsVO`
- 服务：`CodeChallengeService` / `CodeChallengeServiceImpl`
  - 判分模式与代码题库一致：前端跑用例上报 total/passCount，后端判定通关
  - 星级：1 次通过 3 星，2-3 次 2 星，≥4 次 1 星（仅首通计星计分）
  - 积分：3 星满分 / 2 星 80% / 1 星 60%，首通积分同步累加到用户 `exp`
  - 解锁：提交时校验上一关已通关；详情接口按顺序计算 locked
  - 排行榜：按用户聚合积分（同分比星星），支持单赛道榜与总榜
- 控制器：`CodeChallengeController`（`/api/challenges`）
  - GET `/api/challenges`、`/{id}`、`/leaderboard` 匿名可读（SecurityConfig 放行，登录后附带个人进度）
  - POST `/{challengeId}/levels/{levelId}/submit`、GET `/my/stats` 需登录

### 前端（Vue 3 + TS + Pinia）
- `api/challenge.ts` + `api/types.ts` 新增 6 个 Challenge* 类型
- 路由：`/challenge`（列表，匿名可看）、`/challenge/:id`（闯关，需登录）
- `CTopNav.vue`：桌面端「学习工具」下拉与移动端菜单新增「编程挑战」（rocket 图标）
- `Challenge.vue`：我的战绩卡 + 赛道卡片（进度条/星星/积分/参与人数）+ 积分排行榜 + 玩法说明
- `ChallengePlay.vue`：关卡地图（锁定/通关/当前态 + 星级展示）+ 题目面板（示例/提示折叠）+ 代码编辑器（Tab 缩进/重置模板/恢复上次代码）+ 浏览器端判题 + 通关弹窗（星级/积分/下一关）

### 校验
- `mvn compile` 通过；`vue-tsc --noEmit` 通过
- 接口自测：列表/详情（10 关、第 2 关默认锁定）/排行榜均返回正常
- 前后端已重启：后端 8080、前端 Vite dev server

## 2026-07-30 修复前端生产构建（npm run build 闭环）

- 现象：`npm run build`（`vue-tsc -b && vite build`）此前失败，无法产出 `dist/`，阻塞部署。
- 根因两类：
  1. `vue-tsc -b` 严格类型检查约 60+ 处 TS 错误（未使用变量/参数、类型不匹配、可选属性访问、`<script setup>` 误删被模板使用函数等），分布在约 30 个文件（含 B/C 引入的 `KnowledgeGraph`/`LearningReport`/`SmartWriting` 与历史遗留的 `DocEdit`/`Favorites`/`FlashCards`/`KBTitle`/`KnowledgeHome`/`NoteEdit`/`PathDetail`/`Profile`/`SearchResult`/`TaskCenter` 等）。
  2. Vite 8 的 lightningcss 在 minify 阶段报非法 CSS 选择器 `.[-:T]{ -: T; }`：`MyFlashcards.vue` 中日期格式化正则 `/[-:T]/g` 被 Tailwind 按原始文本扫描，误当作「任意属性类」`[-:T]` 生成无效 CSS。
- 修复：逐文件修复 TS 错误；将正则拆为 `.replace(/-/g, '').replace(/:/g, '').replace(/T/g, '')` 以消除连续 `[-:T]` 子串。
- 校验：`npm run build` 退出码 0，`dist/` 正常产出（仅剩 chunk 体积 >500 kB 的非致命警告，可后续用动态 `import()` 做代码分割优化）。

## 2026-08-03 编程 Agent 扩展：本地 Ollama（deepseek-coder）生成代码并保存到指定目录

### 背景
原 `CodeAgent` 仅支持「对话 + 手动提取代码 + 手动另存」，用户说「替我写一个 html demo 案例」时，
需要自己复制代码、新建文件、选路径保存。本次打通「自然语言指令 → 生成 → 落盘」全链路。

### 后端（Spring Boot 3 + JDK HttpClient）
- `dto/CodeGenDTO`：生成请求（prompt / baseUrl / model / temperature），带 `@NotBlank` 与长度校验
- `vo/GeneratedFileVO`、`vo/CodeGenResultVO`：产物文件（fileName/language/content/size）与生成结果
- `service/CodeGenService`：核心生成与解析
  - 调用 Ollama `/api/chat` **非流式**接口（避免代码块被 SSE 分片截断），超时 5 分钟
  - 强约束 system 提示词，要求模型按 `FILE: 文件名` + 围栏代码块输出
  - **三级降级解析**：①带 FILE 标注的代码块 → ②裸代码块按语言推断文件名（同语言追加序号防覆盖）→ ③整段裸 HTML 兜底
  - 安全：文件名做路径穿越（`..`、绝对路径、`:`）与非法字符校验，拒绝后跳过；单文件 200KB、单次 20 文件上限
  - 模型选择：请求指定 > 用户默认模型（仅当含 `coder`）> 内置 `deepseek-coder:6.7b`
  - 异常分类映射为可操作提示：连接失败→提示 `ollama serve`；404→提示 `ollama pull <model>`；超时→提示模型加载中
- `controller/CodeGenController`（`/api/code-gen`）
  - POST `/generate` 生成代码文件
  - GET `/health` **生成前环境自检**（服务是否可达 / 模型是否已安装，返回 hint 修复建议），
    避免用户等待数分钟后才拿到超时错误
- 单元测试 `CodeGenServiceParseTest`：覆盖三级降级、加粗/中文冒号变体、路径穿越拦截、说明文字提取，7 项全通过

### 前端（Vue 3 + TS）
- `api/types.ts` 新增 `CodeGenRequest` / `GeneratedFile` / `CodeGenResult` / `CodeGenHealth`
- `api/codeGen.ts`：`generate`（**单独设置 5 分钟超时**，覆盖 axios 默认 15s，否则本地推理会被提前掐断）、`health`
- `utils/fileSaver.ts`：目录选择与落盘工具
  - 基于 **File System Access API**（`showDirectoryPicker`），支持一层子目录逐级创建
  - 用户取消抛哨兵错误 `UserCancelledError`，与真实失败区分开
  - 不支持的浏览器（Firefox/Safari）**降级为逐个文件下载**
  - 写入前二次校验文件名（纵深防御），`finally` 中确保关闭写入流
- `views/CodeAgent.vue`
  - **意图识别**：「动作词（写/生成/做…）+ 产物词（html/页面/demo…）」双命中，或显式「保存到目录」表述才触发，降低误触发；提供开关「本地生成并保存文件」可关闭
  - 生成全流程：环境自检 → 调用模型 → 解析校验 → 弹目录选择 → 逐文件写入 → 结果反馈
  - 产物卡片：列出文件名/大小，点击可载入右侧编辑器预览，支持「重新保存到目录」（取消后无需重新生成）
  - 进度提示：阶段文案 + 旋转指示器 + 保存进度（已完成/总数），并适配 `prefers-reduced-motion`
  - 异常兜底：模型无有效代码块时展示原始输出供手动复制并给出改写建议；部分文件写入失败时如实汇总成功/失败明细

### 校验
- `mvn compile` 通过；`mvn test -Dtest=CodeGenServiceParseTest` 7/7 通过
- `vue-tsc --noEmit` 通过

## 2026-08-03 编程 Agent 体验优化：目录记忆与结构化推理过程

### 背景
上一版每次生成都会弹出目录选择框，且只有一行进度文案。本次对标主流智能编程工具，补上两点体验能力：①选定目录后自动记忆、后续免重复选择；②清晰呈现「意图理解 → 目录选择 → 代码生成 → 文件保存」的完整推理链。

### 前端（Vue 3 + TS）
- `utils/fileSaver.ts`
  - 新增 **IndexedDB 持久化目录句柄**（`rememberDirectory` / `getRememberedDirectory` / `clearRememberedDirectory`），句柄而非路径，可跨刷新复用
  - `saveFilesToDirectory` 支持 `options.preferDir`（复用记忆目录，跳过弹框）与 `options.remember`（保存后记住），写入前用 `queryPermission`/`requestPermission` 重新授权，用户拒绝授权则回退弹框
  - 返回 `reusedMemory` 标志，供上层区分「复用记忆」与「新选目录」
- `views/CodeAgent.vue`
  - **目录记忆**：启动时 `onMounted` 从 IndexedDB 恢复记忆目录名（展示用），保存成功后同步 `rememberedDirName`；输入工具栏与产物区展示默认目录，并提供「更换目录」/`✕` 一键清空记忆
  - **结构化推理过程**：新增 `reasoningSteps`（理解意图 / 确定目录 / 模型生成 / 写入文件四步），每步含 icon、标题、状态（pending/active/done/error）与细节文案；生成链路逐步更新状态，失败时标记断点步骤；对话区以步骤链卡片实时渲染，含 `prefers-reduced-motion` 适配
  - `buildIntentDetail` 从用户输入提取动作、产物类型、是否要求保存、目标目录，作为「理解意图」步骤细节
  - 目录步骤细节区分「复用记忆目录」与「将弹框选择」；保存成功文案标注是否复用记忆目录

### 校验
- `vue-tsc --noEmit` 针对本文件无新增类型错误（项目其余文件存在与本次无关的预存 TS 报错）

## 2026-08-03 编程 Agent 再优化：设置预配置默认目录 + HTML/JS 分文件生成

### 背景
用户希望「一句话全自动」：在设置中预先配置默认保存路径后，输入「替我实现一个番茄钟」即自动生成完整前端代码（HTML + 独立 JS）并保存到本地目录，全程无需手动操作。

### 后端（CodeGenService.java）
- 强化 `SYSTEM_PROMPT` 的**结构拆分原则**：前端功能类需求（番茄钟、计时器、小游戏、待办、表单等）默认拆为 `index.html`（内联 CSS，通过 `<script src="app.js">` 引入）与独立 `app.js`（承载全部交互逻辑，禁止业务 JS 内联进 HTML）；仅简单静态页退化为单 index.html。新增对应示例输出格式，提高可解析率。

### 前端（Vue 3 + TS）
- 新增**设置抽屉**（`showSettings` + `settings-drawer`）：入口为工具栏「设置」按钮；含「默认保存目录」配置区——显示当前目录、`选择默认目录` 按钮（`pickDefaultDirectory` 调 `pickDirectory` + `rememberDirectory` 持久化）、`清除` 按钮；以及「自动生成与保存」开关。
- 目录句柄类型 `FileSystemDirectoryHandleLike` 从 `fileSaver.ts` 导出，供 `cachedDirHandle` 复用；原加载项目目录的局部函数 `pickDirectory` 改名 `pickProjectDirectory`，避免与 `fileSaver` 的同名导入冲突。
- 生成流程：已配置默认目录时「确定目录」步骤文案明确为「已使用设置的默认保存目录，全程无需手动选择」；保存阶段经 `preferDir` 复用句柄跳过弹框，实现一句指令全自动落盘。
- 样式：新增工具栏设置按钮、抽屉布局、`drawer` 过渡动画，均复用 `--kb-*` 变量并适配 `prefers-reduced-motion`。

### 校验
- `vue-tsc --noEmit` 针对本文件无新增类型错误；无 lint 错误

## 2026-08-03 编程 Agent 再优化：文件树自动刷新与目录同步

### 背景
用户希望代码生成并落盘后，IDE 内的项目目录结构自动更新，新文件/文件夹/修改立即在文件树可见，无需手动刷新；并支持监听文件系统变化、可配置启用/禁用与刷新延迟。

### 前端（Vue 3 + TS）
- 新增刷新与监听状态：`autoRefreshTree`（默认开）、`refreshDelay`（默认 800ms，合并多文件触发）、`watchTree`（轮询监听，默认关）。
- `refreshFileTree()`：从 `rootHandle` 全量重建文件树，并通过 `applyExpandedState()` 按 `path` 递归保留已展开目录的展开状态，使新文件/文件夹立即可见；沿用 `IGNORED_DIRS` 兼容多项目类型与深层目录。
- `scheduleRefresh()`：防抖触发（受 `autoRefreshTree` / `refreshDelay` 控制），生成多文件时合并为一次更新。
- 目录监听：`tickWatch()` + `isSameStructure()` 周期性（间隔取 `max(refreshDelay, 1500)`）对比根目录条目名，变化时刷新；基于 `watch(rootHandle)` / `watch(watchTree)` 自动启停，组件 `onUnmounted` 清理定时器。`FileSystemObserver` 因跨浏览器不稳定未采用，改用轻量轮询模拟 watch。
- 保存成功后（directory 模式且已挂载项目目录）调用 `scheduleRefresh()`，并在推理步骤标注「文件树将在 Xms 后自动刷新」。
- 设置抽屉「文件树自动刷新」区：自动刷新开关、持续监听开关、刷新延迟滑块（200–3000ms，实时显示数值）。
- 文件树头部新增手动「刷新」按钮（`refresh-cw` 图标），仅挂载目录时显示。
- 样式：复用 `--kb-*` 变量，新增手动刷新按钮与延迟滑块样式。

### 校验
- `vue-tsc --noEmit` 针对本文件无新增类型错误；无 lint 错误。
- 注：自动刷新仅在「默认保存目录 == 挂载的项目目录」时能看到文件树变化；否则仅静默刷新挂载目录（无变化）。
- 样式统一使用既有 `kb-*` 变量（`--kb-background`/`--kb-foreground`/`--kb-muted-foreground`/`--kb-accent` 等），图标复用 `Icon.vue`（folder/file/download/check）

## 2026-08-03 编程 Agent 意图识别与答案生成优化：前端接入 + 链路打通（方案 P1~P3 落地）

### 背景
后端已先行落地 `IntentService`（多轮 LLM 意图分类 + 结构/语义歧义检测 + 准确率评估）、`IntentKnowledge`（语言/框架/场景规则表）、`AgentIntentController`（`/api/agent/intent`、`/ambiguities`、`/evaluate`）与 `AgentCallLog.score` 字段；前端 `CodeAgent.vue` 与 `api/codeAgent.ts` 此前仍是旧链路（正则二分类 + 命中即执行）。本次把前端切到新后端，打通「意图识别 → 路由 → 确认 → 歧义标注 → 评估回流」全链路，后端无需改动。

### 前端（Vue 3 + TS）
- `send()` 改造：废弃 `detectCodeGenIntent` 正则二分类，改先 `codeAgentApi.detectIntent(buildIntentRequest())` 拿结构化意图，据 `intent`（generate/modify→本地生成，explain/debug/chat→SSE）路由；意图服务异常时降级为正则兜底，保证基础对话/生成可用。
- 新增 `buildIntentRequest()`：组装近 6 轮（带 `intent/slots` 回填）历史 + 挂载目录快照 `projectSnapshot`（由 `fileTree` 拍平，供后端结构探针做歧义检测）。
- `routeByIntent()`：统一路由入口，回填 `message.intent/slots` 供下一轮上下文消解；`needsClarify` 时冻结输入并展示结构化澄清卡片，用户作答/直接执行后重进识别。
- 显式确认（P2）：新增 `pendingClarify` / `clarifyText` / `lastIntent` 状态；澄清卡片展示 `clarifications[].question/options`，作答以带意图的 user 消息追加；推理步骤链新增「澄清意图」节点（help-circle 图标）。
- 歧义检测（P2）：`runCodeGeneration` 生成后调 `codeAgentApi.detectAmbiguities`，结果写入 `message.ambiguity`，产物区以 `--kb-warning` 高亮标签展示（kind/reason/suggestion）。
- 准确率评估（P3）：生成/回答完成后调 `codeAgentApi.evaluate`，回填 `message.evalScore`，消息区展示「匹配度」徽标（按高/中/低分三档配色，复用 `--kb-accent`/`--kb-warning`/`--kb-danger`）。
- `runCodeGeneration` 支持 `modify` 模式：把上一轮产物文件拼入 prompt 实现「在它基础上改」；`intent/slots` 回填到生成消息。
- `ChatMessage` 复用既有 `intent/slots/ambiguity/evalScore` 字段；类型对接 `types.ts` 的 `AgentIntentRequest/AgentIntentResult/Ambiguity/ClarifyQuestion/ProjectSnapshotItem/IntentHistoryItem`。

### 涉及文件
- `frontend/src/views/CodeAgent.vue`（核心改造）
- `frontend/src/api/types.ts`、`frontend/src/api/codeAgent.ts`（类型/接口已先行就绪，本次仅对接）

### 校验
- `vue-tsc --noEmit` 无新增类型错误；IDE lint 0 错误。
- 后端 `IntentService`/`AgentIntentController` 已在本次会话前置提交中落地，未改动后端即完成链路打通。
- 待联调：本地 Ollama 启动且 `deepseek-coder` 已安装时，验证「生成 → 澄清（低置信度）→ 歧义标签 → 匹配度徽标」全链路。

---

## 2026-08-04 编程 Agent 内核：多 Provider 适配层 + 工具调用引擎（P0~P1）

### 背景
在已有 `CodeAgentController` / `AiService`（仅 OpenAI 兼容）/ `agent_session` / `agent_message` 地基上，补齐「完整编程 Agent」两大内核：①**真·多协议模型适配层**（OpenAI / Anthropic / 文心千fan 各自原生协议，屏蔽差异）；②**工具调用引擎**（动态注册、权限校验、ReAct 编排、调用链落库）。前端按约定本次暂不改动（分批：先后端内核）。

### 后端（Spring Boot 3 + MyBatis-Plus）

**P0 多 Provider 统一适配层**
- `config/AiProviderRegistry`：`ProviderInfo` 新增 `Protocol`（OPENAI/ANTHROPIC/QIANFAN），注册时按 provider 设定（anthropic→ANTHROPIC，wenxin→QIANFAN，其余→OPENAI）。
- `ai/ModelAdapter`（接口 + `ChatMessage`/`ToolSpec`/`ToolCall`/`ChatRequest`/`TokenDelta`/`StreamDone`/`ChatResult`）：统一对话/工具调用数据结构。
- `ai/OpenAiAdapter`：沿用既有 `/v1/chat/completions` 逻辑，支持 tools/tool_calls + SSE。
- `ai/AnthropicAdapter`：POST `/v1/messages` + `x-api-key` + `anthropic-version`，解析 `tool_use` 与 `content_block_delta` 流式。
- `ai/QianfanAdapter`：用 `apiKey+secret` 换 `access_token`，POST 对话接口，tool 走 `functions`（受限）。
- `ai/SseClient`：轻量 SSE 流式 HTTP 客户端（替代每协议重复实现）。
- `ai/ModelAdapterFactory`：按 `registry.find(provider).getProtocol()` 选择 Adapter。
- `service/impl/AiServiceImpl`：`streamChat`（带参重载）改为委托 Adapter（Controller/前端零改动）；新增 `chatMulti`/`streamMulti` 多轮 + 工具通道；`healthCheck` 走 Adapter；`EffectiveAiConfig` 扩展携带 `provider`/`apiSecret`。
- `entity/UserAiConfig` 加 `apiSecret`（文心鉴权）。

**P1 工具调用引擎**
- `agent/tool/AgentTool`（接口）、`ToolContext`、`ToolResult`、`ToolPermission`（SAFE/WRITE/DANGEROUS）、`ToolException`：工具抽象与运行上下文。
- `agent/tool/ToolRegistry`：自动收集 Spring Bean、按用户配置判定启用/授权、权限校验、参数合法性、调用链落库（`agent_tool_call`）。
- 内置工具（`agent/tool/builtin/`）：`CodeRunTool`（复用 `CodeExecutionService`，SAFE 默认启用）、`FsReadTool`（SAFE 默认启用）、`FsWriteTool`（WRITE 默认禁用）、`DbQueryTool`（只读 SELECT，DANGEROUS 默认禁用）。
- `agent/AgentRuntimeService`：ReAct 编排（载历史→注入工具→`AiService.chatMulti`→解析 tool_calls→`ToolRegistry.invoke`→回灌→上限 8 轮）。
- `controller/AgentToolController`：`GET /api/agent/tools`（列表）、`GET /{name}`、`PUT /{name}`（启停/授权）、`GET /sessions/{id}/call-chain`（调用链可视化数据源）。
- `controller/CodeAgentController` 新增 `POST /api/agent/chat`（同步 ReAct 对话，落库 user/assistant 消息）。
- 持久化：`agent_tool_config` / `agent_tool_call` 两表（`schema.sql` + 实体 + Mapper）；`agent_message`/`agent_session` 扩展 `message_type`/`tool_call_id`/`tool_name`/`parent_id`/`context_window`/`agent_mode`。
- `exception/GlobalExceptionHandler` 增加 `AiException` 统一映射（业务码 5000 段）。

### 设计决策
- Adapter 模式 + Factory 路由，新增 provider 只需加 `Protocol` 枚举值 + 一个 Adapter 实现，符合开闭原则。
- WRITE/DANGEROUS 默认禁用，需用户显式授权（`allow_write=1`）才执行；二次交互确认（tool-confirm 事件）留待 P4 前端阶段。
- 文心 tool_calling 各模型能力不一，本期标注「受限」，仅文本多轮保证可用。
- `DbQueryTool` 仅开放只读 `SELECT` 单语句，禁止多语句与 DDL，防越权。

### 涉及文件
- 新增：`ai/` 5 个类、`agent/` 与 `agent/tool/` 9 个类、`controller/AgentToolController.java`、`entity/AgentToolConfig(AgentToolCall).java`、`mapper/AgentToolConfig(AgentToolCall)Mapper.java`
- 修改：`AiProviderRegistry`、`AiServiceImpl`、`UserAiConfig`、`AgentMessage`、`CodeAgentController`、`GlobalExceptionHandler`、`schema.sql`、`AiService` 接口
- 文档：`DATABASE.md`（45→47 表 + 2 新表说明）、`README.md`（控制器清单 + 适配层/工具引擎说明）

### 校验
- 后端 `mvn compile` 通过；`Tomcat started on port 8080` + `Started KnowFlowApplication`，新表随 H2 初始化。
- 接口自测：服务启动无 bean 装配错误；`/api/agent/tools` 鉴权拦截正常（401）；ReAct 全链路需有效模型配置后联调（本批未碰前端）。
