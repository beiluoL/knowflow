# 个人本地部署 · 文件存储选型建议（2026-08-02）

## 用户前提
- 使用场景：**个人本地单机使用**，非多人/非云部署。
- 愿意把存储从 H2 改为 **MySQL**（驱动已在 `pom.xml:56` 存在，仅改配置）。
- 核心诉求：**导入速度快** + **有真实进度条**。

## 结论（直接给）
**采用方案一：本地文件系统存文件实体 + MySQL 只存路径与元数据。**
附带一个**极薄的 `StorageService` 抽象层**（可选，但不增加多少成本，给将来留退路）。

**不要**为个人本地用上：DB BLOB、MinIO、CAS 去重对账那套重型方案。
（方案二从"跑不起来"变成了"能跑但没必要"；MinIO/分布式 FS 对个人是杀鸡用牛刀。）

## 为什么是方案一（修正此前 H2 视角）
切到 MySQL 8.0 后，方案二（BLOB 入库）**不再是技术不可行**，需如实修正此前"3 倍写放大、缓冲池必爆"的偏激说法：
- MySQL 8.0 默认 `DYNAMIC` 行格式：大 BLOB 只把前 768 字节（实际 20 字节指针）放聚簇索引，主体存 **off-page**，缓冲池污染远小于旧格式；
- InnoDB redo log **不记录 BLOB 主体**，只是页变更，因此存储端写放大没有之前说的那么夸张。

但即便可行，个人本地用仍应避开 BLOB，理由更务实：
1. **备份膨胀**：`mysqldump` 把 BLOB base64 编码，dump 文件体积 +30% 以上，恢复慢；个人备份从"拷文件夹"变成"导库"，心智负担陡增。
2. **`max_allowed_packet` 暗坑**：必须调大到大于你最大的单个文件，否则大文件静默失败。
3. **文件不可直接操作**：没法 `ls`/`cp`/`rsync`/`open`，所有访问被绑死在应用内。
4. **导入速度**：文件流式落盘（走 OS page cache）天然快于"经 MySQL 协议 → base64 解码 → 入行"。批量导目录时差距明显。

## 导入速度 + 进度条怎么落地（针对本地）
- **分批逐文件上传**，不要像现在 `KnowledgeImport.vue:548` 把整目录塞一个 FormData（500MB 上限一超就 413，且假进度）。
- **并发 3~4** 上传单文件，每个文件用 axios `onUploadProgress` 累加 `loaded` 到一个 `Map<index, loadedBytes>`，总进度 = 已累加 / 总字节。**这是本地能拿到的最真实进度条**，无需分片。
- **分片上传 / 断点续传：个人本地基本不需要**。localhost 上传稳定性极高，分片只增加复杂度。只有当你要导 **>2GB 的单个大文件** 时才考虑加分片；届时协议复用之前方案里的四段式即可，但属可选增强而非必需。
- 强烈建议**顺手修** `KnowledgeImportServiceImpl:702` 的 `readContent()` 按 UTF-8 纯文本读所有文件 → PDF/DOCX 现在导入是乱码，存储再快内容不可用也白搭（按扩展名选 Tika/POI 解析）。

## 切到 MySQL 的落地步骤（配置级，无新依赖）
1. 新建 `application-mysql.yml`（或改 `application.yml` 的 `spring.datasource`）：
   - `driver-class-name: com.mysql.cj.jdbc.Driver`
   - `url: jdbc:mysql://localhost:3306/knowflow?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai`
   - 账号/密码 + 建库 `CREATE DATABASE knowflow`。
2. 启动参数加 `--spring.profiles.active=mysql`（或环境变量 `SPRING_PROFILES_ACTIVE=mysql`，见 `application.yml:8`）。
3. **校验 `schema.sql` 在真 MySQL 可跑**：当前为 H2 `MODE=MySQL` 兼容写法，需注意 `AUTO_INCREMENT`、布尔 `TINYINT`、索引定义等是否与 MySQL 完全一致（建议先在本地 MySQL 跑一遍建表验证）。
4. 应用层把"物理路径"与"用户目录树"解耦：落盘用扁平目录（如 `uploads/<yyyyMM>/<uuid>.<ext>`），目录结构只存 DB（`doc_category` + 新增 `relative_path` 列），避免同名覆盖 / 路径穿越 / 单目录文件过多。

## 可选增强（非必需，按需）
- **轻量去重**：若你的知识库目录里同一 PDF 散落多个子目录（常见），可在 `doc_document` 加 `file_hash` + 引用计数，重复文件只存一份。但**不用上每日对账定时任务**——个人用，手动跑一次去重脚本即可。
- **`StorageService` 抽象**：定义 `save(InputStream, hash) / get(path) / delete(path)`，默认实现是本地 FS；将来真要上云，换一个 Bean 即可，业务代码零改。

## 一句话
个人本地 + MySQL：**文件落本地磁盘、路径存 MySQL、逐文件并发上传带真进度条**。BLOB/MinIO/CAS 重型方案一律不上，保持简单 = 最快也最好维护。
