# KnowFlow 代码库全面审查报告

> 生成时间：2026-07-28 ｜ 审查方式：静态代码审计（3 个并行探查 agent 覆盖后端接口 / 前端页面 / 代码质量）+ 运行态接口探测 + 生产构建亲自验证
> 审查范围：后端 `backend/`（Spring Boot 3 + MyBatis-Plus + JWT）、前端 `frontend/`（Vue 3 + TS + Vite）

---

## 0. 执行摘要（最重要）

| 维度 | 状态 | 说明 |
|---|---|---|
| 后端编译 | ✅ 通过 | `mvn compile` 通过，无编译错误 |
| 后端运行 | ✅ 运行中 | 本地 `:8080` 已启动，新接口已在线验证（知识图谱返回真实分类/文档节点） |
| 前端开发服务器 | ✅ 可用 | `:5173` 运行中（Vite 不类型检查，页面可点击） |
| **前端生产构建** | ✅ **通过** | `npm run build` 已于 **2026-07-30** 修复并验证通过（消除 `vue-tsc -b` 全部 TS 错误 + Vite 8 lightningcss 非法 CSS 选择器），退出码 0、产出 `dist/` |
| 后端接口质量 | ✅ 高 | 约 78 个端点中 76 个为真实实现（A），**0 个空壳/占位端点** |
| 前端页面质量 | ⚠️ 中等 | 45 个页面：~27 个接真实 API、7 个 mock 空壳、8 个部分实现、1 个编译错误 |

**一句话结论**：功能骨架完整、后端扎实，前端生产构建已于 2026-07-30 修复（可正常打包部署）；B/C 模块仍存真实缺口（掌握分布看板未做、分享仅前端化），需补缺口后上线。

---

## 一、已完成 / 已实现功能清单（按模块）

> 状态图例：**A** = 真实实现（读写真实数据）｜ 标记「需 AI Key」= 代码完整但默认未配置大模型密钥时不可用

### 1. 账号与鉴权（Auth / User）
| 功能 | 状态 | 说明 |
|---|---|---|
| 登录 / 注册 / 登出（JWT 黑名单） | A | BCrypt 加密，登出 token 入黑名单 |
| 当前用户 / 个人资料更新 | A | 实时读写 |
| 用户统计（阅读/收藏/路径/经验/精力） | A | 真实聚合多表，非死数据 |
| GitHub / 微信 OAuth 登录 | A | 完整回调流程（见 P1 风险） |

### 2. 文档与分类（Doc / Category）
| 功能 | 状态 | 说明 |
|---|---|---|
| 文档列表 / 详情 / 搜索 / 分类筛选 | A | 分页 + 关键词 + 难度 |
| 收藏切换 / 取消 + 计数同步 | A | 原子计数 |
| 阅读进度记录 + 完成计数 | A | 原子自增 |
| 最近阅读 / 热度推荐 | A | 真实统计 |
| 分类树（父子嵌套） | A | 递归构建 |
| **AI 摘要生成并落库** | A（需 AI Key） | 调大模型写 `summary` 回写文档 |
| **AI 摘要自动生成闪卡并落库** | A（需 AI Key） | 解析 JSON → 写入 `learning_flashcard` |

### 3. AI 对话（Chat）
| 功能 | 状态 | 说明 |
|---|---|---|
| 会话 CRUD / 消息列表 | A | 真实持久化 |
| 发送消息（RAG 检索 + 大模型回复） | A（需 AI Key） | 未配 key 时优雅降级返回提示文案 |
| **多模型切换（GET /api/chat/models）** | A | 模型列表读配置，发送时透传 `model` 到后端 |
| 分享（复制链接 / 导出 Markdown） | C | 仅前端，无后端分享接口；"生成图片"为占位 |

### 4. 学习中心（Learning）
| 功能 | 状态 | 说明 |
|---|---|---|
| 学习路径 / 章节 / 报名 | A | 报名自动建任务 + 通知 |
| 闪卡列表 + SM-2 间隔复习 | A | 真实算法 |
| 学习任务 CRUD | A | 真实表 |
| **学习热力图数据（按日期聚合）** | A | `GET /api/learning/stats/daily-activity` 聚合三表 |
| **掌握分布数据（闪卡/错题）** | C | 端点存在，但闪卡部分**未过滤 userId**（全站统计，见 P1） |

### 5. 错题本（Mistake）
| 功能 | 状态 | 说明 |
|---|---|---|
| 错题分页 / 详情 / 标记掌握 | A | 鉴权 |
| 新增（幂等去重） | A | 同用户同题自动去重 |
| 统计（total/mastered/pending/weekly/due） | A | 真实聚合 |

### 6. 知识图谱（Knowledge）
| 功能 | 状态 | 说明 |
|---|---|---|
| 图谱节点/边数据接口 | A | 分类节点 + 文档节点 + 父子/归属边，真实表 |
| 前端 SVG 可视化 | A（导出按钮损坏） | 渲染正常；**导出图谱按钮引用未声明变量 → 编译错误** |

### 7. 社区 / 通知 / 管理后台
| 功能 | 状态 | 说明 |
|---|---|---|
| 社区帖子 / 评论 / 点赞 | A | 真实持久化 + 通知 |
| 消息通知（已读/未读） | A | 真实表 |
| 管理概览（总量/增长/健康度/活动流） | A | 实时聚合，非演示 |
| 管理：文档/用户/分类/闪卡 CRUD | A | 用户删除有防误删保护 |
| 管理：知识库/社区/对话配置 | C / B | 部分字段 mock、对话配置仅 localStorage |

---

## 二、未实现 / 开发中功能（目标 + 当前进度）

### B. AI 增强
| 需求 | 目标 | 进度 | 缺口 |
|---|---|---|---|
| B① SmartWriting AI 评分+预览+导出 | 写作智能打分 + 实时预览 + 导出 PDF/MD | **90%** | 功能齐全；AI 评分有 `Math.random()` mock 降级路径；相关类型错误已修复 |
| B② 对话多模型切换 + 分享 | 模型自由选择 + 对话分享 | **70%** | 模型切换完整；分享仅"复制链接+导出 MD"，**无后端分享接口、无生成图片**（占位） |
| B③ 文档 AI 摘要 + 自动闪卡 | 一键生成摘要与复习闪卡 | **85%** | 前后端已接通；**默认未配 AI Key 时不可用**（运行时 D） |

### C. 数据可视化
| 需求 | 目标 | 进度 | 缺口 |
|---|---|---|---|
| C① 学习热力图 | 时间维度学习活跃度 | **100%** | 后端聚合 + 前端 GitHub 式网格已实现 |
| C① 掌握分布看板 | 各知识点掌握度可视化 | **0%** ❌ | **未实现**：`LearningReport.vue` 与 `LearningCenter.vue` 内容近乎逐行相同，未新增掌握度看板；`learningApi.mastery()` 全工程 0 调用 |
| C② 知识图谱 | 知识点关联/层级可视化 | **80%** | SVG 渲染完成；导出图谱按钮的类型错误（未声明 `nodes`/`edges`）已修复 |

### 其它长期 mock / 空壳页面（非本轮目标，但影响"完整性"）
- **B 级空壳（写死假数据）**：CodePractice（8 题写死+假输出）、NotesManage（本地示例）、TaskCenter（写死任务）、CheckIn（本地 mock）、Achievement、KBTitle、admin/TagManagement、admin/ChatConfig。
- **C 级部分**：LearningCenter（含 `<Badge>演示</Badge>` + 本地番茄钟）、admin/Overview（部分图表演示映射）、admin/DocManagement（size/owner mock）、admin/KnowledgeMgmt（memberCount=8 写死）、NoteEdit（笔记落地多为本地）。

---

## 三、已知问题 / 潜在风险 / 技术债

### 🔴 P0 严重（阻断 / 安全 / 数据）
1. ~~**前端生产构建断裂（已亲自验证）**~~ **已修复（2026-07-30）**：`npm run build` 的 ~60+ 处 `vue-tsc -b` TS 错误（`KnowledgeGraph`/`LearningReport`/`SmartWriting` 及历史遗留的 `DocEdit`/`Favorites`/`FlashCards`/`KBTitle`/`KnowledgeHome`/`NoteEdit`/`PathDetail`/`Profile`/`SearchResult`/`TaskCenter` 等）与 Vite 8 lightningcss 非法 CSS 选择器（源于 `MyFlashcards.vue` 日期正则被 Tailwind 误提取）均已修复，现构建退出码 0、可部署。
2. **H2 控制台公开**（P0-1）：`application.yml:21` 启用 + `SecurityConfig` `permitAll` `/h2-console/**`。内存库 `sa` 空密码，可任意执行 SQL，历史上有 RCE 风险。生产必须关。
3. **JWT 默认密钥硬编码**（P0-2）：`jwt.secret` 默认值写死在 yml，未设 `JWT_SECRET` 时可被伪造任意身份（含 ADMIN）令牌。应 fail-fast 拒绝启动。

### 🟠 P1 中等（安全配置 / 一致性 / 性能）
4. **XSS - Markdown 链接无协议白名单**：`utils/markdown.ts` 的 `escapeHtml` 不转义引号，`[x](javascript:alert(1))` 可被执行；`DocEdit.vue`/`DocCreate.vue` 预览 `v-html` 完全不转义 HTML（存储型 XSS，管理员编辑他人文档时中招）。`DocDetail.vue` 的 `sanitizeUrl` 是正确范本但未复用。
5. **OAuth `state` 未校验 / token 明文进 URL**：GitHub 回调不校验 state（CSRF 绑定攻击）；微信用常量 `state=knowflow`；回调把 JWT 拼进 URL（`?token=`）→ 进历史/日志泄露。
6. **CORS 通配 + allowCredentials**：`CorsConfig` 允许 `*` 源带凭证，当前鉴权基于 Bearer 危害有限，但属高危配置。
7. **计数 read-modify-write 非原子**：`ChatServiceImpl`/`MistakeServiceImpl`/`LearningServiceImpl` 先读后改再 `updateById`（与项目其它处原子 `setSql` 不一致），并发丢更新。
8. **删除用户未级联清理子表**：`AdminUserController` 直接 `removeById`，产生孤儿帖/会话/学习记录（违反项目"逻辑外键需清理"约定）。
9. **`stats/mastery` 闪卡统计忽略 userId**：`LearningServiceImpl:352` 全站统计，非"我的掌握分布"。
10. **N+1 查询**：`AdminOverviewServiceImpl:159` 循环内 `userMapper.selectById`。
11. **异常被吞**：`DocServiceImpl` 闪卡 JSON 解析失败仅打日志返回空，调用方无感知。
12. **Swagger/OpenAPI 未鉴权暴露**：`/v3/api-docs/**` 等 `permitAll`（配合 P0-2 更危险）。

### 🟡 P2 轻微（可维护性 / 坏味道）
13. `@PreAuthorize` 不一致：仅部分 admin 控制器标注，其余仅靠 URL 级守护（纵深不足）。
14. MyBatis stdout SQL 日志全环境开启（生产噪声 + 敏感参数落盘）。
15. 前端死按钮/占位：`DocDetail` 分享、`CommunityManage` 新建话题、`DocEdit` 版本历史、`Chat` 生成图片 均 `notify('...开发中')`。
16. TS `any` 滥用（`Docs.vue`/`Profile.vue`/`KnowledgeHome.vue`）。
17. 注册密码策略弱（仅 `@Size(6,20)`，无复杂度/二次确认）。
18. 魔法数字（JWT 过期、LIMIT 阈值等未抽常量）。

---

## 四、建议修复优先级路线图

| 阶段 | 动作 | 必要性 |
|---|---|---|
| ~~**紧急**~~ **已完成** | 修前端生产构建（vue-tsc + lightningcss），2026-07-30 已闭环 | 上线前置，现已满足 |
| **紧急** | 关 H2 控制台 + JWT 默认密钥 fail-fast | 安全红线 |
| **高** | 补 C① 掌握分布看板（真实调用 `mastery` + `mistakes.stats`，区分 LearningReport 与 LearningCenter） | 本轮明确需求缺口 |
| **高** | 修 KnowledgeGraph 导出按钮（用 `graph.value.nodes/edges` 或 `renderNodes/renderEdges`） | 本轮引入的编译错误 |
| **高** | XSS 治理（统一 `sanitizeUrl`/DOMPurify，先 escape 再渲染） | 安全 |
| **中** | OAuth state 校验 + token 用 postMessage/HttpOnly | 安全 |
| **中** | 计数原子化、删除用户级联、mastery 加 userId、N+1 批量查 | 一致性/性能 |
| **低** | 清理 mock 空壳页（CodePractice/NotesManage/TaskCenter 等）、死按钮、as any、stdout 日志 | 完整性/可维护性 |

---

## 五、当前运行态（供本地验收）
- 后端：`http://localhost:8080`（已验证知识图谱接口返回真实数据）
- 前端：`http://localhost:5173`（dev 模式，可点击；`npm run build` 生产构建已于 2026-07-30 修复，产出 `dist/`）
- ⚠️ 注意：H2 为内存库，重启数据重置为 `data.sql` 种子；AI 相关功能需配置 `AI_API_KEY` 环境变量后生效。
